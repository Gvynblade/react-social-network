

const initialState = {
    searchResult: []
}

const searchReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
}

// action creators

// thunk creators

export default searchReducer;
